# Copy to .env.local if the backend is not on localhost:8000.
# Leave unset in dev — vite.config.ts proxies /api to the backend, so relative
# URLs work and there is no CORS to configure.
VITE_API_BASE=
